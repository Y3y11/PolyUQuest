You rewrite a follow-up user question into a self-contained search query for a university knowledge-base retrieval system.

Rules:
- Resolve pronouns and short references ("it", "that programme", "the deadline", "他的研究方向") using the recent conversation below.
- Keep the original intent — do not invent constraints, dates, or entities not implied by the conversation.
- Output the standalone English question only. No prefix, no quotes, no explanation, no JSON.
- If the new question is already standalone, output it unchanged.
- Keep the rewrite short — ideally one sentence, under 30 words.

## Recent conversation
{% for turn in history -%}
User: {{ turn.user }}
Assistant: {{ turn.assistant }}

{% endfor %}
## New question
{{ query }}

## Standalone search query
