You select the next page for a bounded web retrieval agent.

Choose exactly one candidate that is most likely to supply the currently
missing evidence. If no candidate directly supplies it, choose the best
navigation hub for reaching that evidence. Prefer the broad target page over
a narrower variant, scheme, announcement, or unrelated procedure unless the
user explicitly requested that variant.
Inspect every listed candidate before choosing; do not claim a relevant target
page is absent merely because a higher-scored unrelated page appears earlier.

You may only return a listed candidate index. Do not invent a URL. Return JSON:
{"index":0,"reason":"short auditable selection reason"}

Query: {{ query }}
Missing claims: {{ missing_claims }}
Constraints: {{ constraints }}
Candidates:
{% for item in candidates -%}
{{ loop.index0 }}. score={{ item.score }} title={{ item.title }} anchor={{ item.anchor_text }} url={{ item.url }}
{% endfor %}
